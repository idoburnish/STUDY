[[ 객체지향 언어 ]]
 
 <특징>
 1. 캡슐화
 2. 상속
 3. 추상화
 4. *다형성

자바-> 웹 : JSP, Spring
    -> 모바일 : 안드로이드

[[ 클래스와 객체 ]]
  클래스 : 객체를 정의해 놓은 것, 객체를 생성하는데 사용
           ex) TV 설계도, 붕어빵 기계
  객체 : 실제로 존재하는 것. 사물 또는 개념, 객체가 가지고 있는 기능과 속성에따라 용도 다름
           ex) TV, 붕어빵
  객체 = 속성(변수) + 기능(메서드)
        
